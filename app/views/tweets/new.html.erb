<% if user_signed_in? %>
  <%= form_for :tweet, :url => tweets_create_path, :method => :post do |f| %>
    <%= f.text_area :message %>
    <br/>
    <%= f.submit "Post Tweet" %>
  <% end %>
<% else %>
  Please sign in.
<% end %>
