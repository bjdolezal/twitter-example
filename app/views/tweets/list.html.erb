<%
# ================================================
# VIEW ===========================================
# ================================================
%>

<% @tweets.each do |tweet| %>
  <b><%= tweet.user.email %></b>
  <br/>
  <%= tweet.created_at %>
  <br/>
  <%= tweet.message %>
  <br/>
  <%= link_to "Delete", tweets_delete_path(tweet), :method => :delete %>
  <hr/>
<% end %>
